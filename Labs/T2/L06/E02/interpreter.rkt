; Write a map primitive for racket-1 (call it map-1 so you and Racket don't get confused 
; about which is which) that works correctly for all mapped procedures.

#lang racket

(require "./racket1.rkt")

(racket-1)

